version: "3"
services:
  app:
    container_name: nodejs
    restart: always
    build: ./
    ports:
      - "8080:51005"
    volumes:
      - .:/home/nodejs/app
    links:
      - mongo
  mongo:
    container_name: mongo
    image: mongo
    ports:
      - "27017:27017"